# Kernel source drop (357 MB) and everything unpacked from it
linux-source-6.8.0_*.deb
linux-source-6.8.0/
deb/

# Everything under build/ is regenerated by build.sh: staged kernel sources,
# the generated Makefile and the module build output.
build/
